namespace Simply_Snake_Game.Enums
{
    public enum Difficulty
    {
        Easy = 100,     // no borders, can hit Self,     const speed
        Medium = 70,    // borders,    can not hit Self, const speed
        Hard = 50       // borders,    can not hit Self, speed acceleration
    }
}
